listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/

# This development configuration is intentionally LAN-only/no-auth. Add a
# password file and TLS before exposing the broker outside a trusted network.

